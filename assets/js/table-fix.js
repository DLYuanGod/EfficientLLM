document.addEventListener('DOMContentLoaded', function() {
  // 获取表格
  const table = document.getElementById('efficiency-table');
  if (!table) return;
  
  // 禁用原有的表格修复逻辑
  // 不再自动填充空白单元格
  
  console.log('表格数据保持原样，不进行自动填充');
}); 